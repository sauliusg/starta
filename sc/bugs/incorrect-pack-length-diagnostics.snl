// --*- C -*--

// Test unsigned values

use std;
use unsigned;

var i : array of short;
var u : array of ushort;

i = [ 1S, 3S, -1S ];

var b = new blob(8);
. length(i) * const(short.size);
pack b, length(i), "i2", i;
