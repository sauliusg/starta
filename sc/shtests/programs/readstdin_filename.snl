// --*- C -*--

// Test while(<>) construct:

pragma prepend "$D";

use std;

while(<>) {
    . "\"" _ "%s" %% currfilename() _ "\"", curreof() ? "TRUE" : "FALSE", $_;
}

. curreof() ? "TRUE" : "FALSE";
