//--*- Java -*--
// Compiler tests
//

use * from std;

var m : int;
var b : bool;

b = true;

function f( x: int ): bool
{
    assert b;
    assert x >= 0;
    return x > 0;
}

assert f(3);

. f(0) ? "T" : "F";
. f(1) ? "T" : "F";
