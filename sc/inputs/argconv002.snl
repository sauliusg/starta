#
# Snail compiler tests
#
#
# Test enumerator types (enum).
#

include "inputs/stdtypes.slib"

function parallel( x, y : float ): float
{
    return ( x * y ) / ( x + y );
}

var a : int = 4;
var b : int = 70;

. parallel( a, b );
