#
# Snail language compiler tests
#
# Test substrings
#

use * from std;

var s = "two";

var s1 = s[2 .. -1];
var s2 = s[3 .. -1];
var s3 = s[2 .. 1];
var s4 = s[3 .. 1];

. length(s);
. s;
. "'" _ s _ "'";
. "";

. length(s1);
. s1;
. "'" _ s1 _ "'";
. "";

. length(s2);
. s2;
. "'" _ s2 _ "'";
. "";

. length(s3);
. s3;
. "'" _ s3 _ "'";
. "";

. length(s4);
. s4;
. "'" _ s4 _ "'";
. "";
