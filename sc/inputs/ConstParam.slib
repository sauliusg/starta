# An example of a module parametrised with a constant, a variable and
# a procedure.

module ConstParam( const C, type T )

use * from std;

var default_size = const(C);

var table = new T[const(C)];

end module ConstParam;
