//--*- C -*--

use std;

var a = [ "vienas", "du" ];
var b = [ "eins", "zwei", "drei", "vier", "fünf" ];

for var l in a do
    . l;
    var count = 0;
    // var m: string;
    for var m in b  {
        . "   ", m;
        count++;
        if( count > 2 ) { break }
    }   
    . "";
enddo
