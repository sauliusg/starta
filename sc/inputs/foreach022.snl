//--*- C -*--

use std;

var a = [ "vienas", "du" ];
var b = [ "eins", "zwei", "drei", "vier", "fünf" ];

var l: string;
for ( l => a ) {
    . l;
    var count = 0;
    var m: string;
    for ( m in b ) {
        . "   ", m;
        count++;
        if( count > 2 ) { break }
    }   
    . "";
}
