
from std use *;

var x, y, z = 1, 2, 3;

var fff = closure( x, y : float ): float
lambda {
    var a : int = x + 1;
    var b : int = y + z;
    var s : string = "va va";
}
{ 
  return x*y/(lambda.a + lambda.b)@float
};

x, y, z = 2, 4, 6;

. fff( 2, 3 );
. fff( 5, 3 );
