#
# Snail compiler tests
#
#
# Test struct and class methods
#

use std;

type A = struct {
    int ii;
    float xx;
}

type C = struct : A {
    int a;
    string name;
}

. "C.ii.offset   =", const(C.ii.offset/.stackcellsize);
. "C.xx.offset   =", const(C.xx.offset/.stackcellsize);
. "C.a.offset    =", const(C.a.offset/.stackcellsize);
. "C.name.offset =", const(C.name.offset/.stackcellsize);
