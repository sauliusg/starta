
from std use *;

var x, y, z = 1, 2, 3;

function sumdiff( x, y : int ): int, int
{
    return x + y, x - y;
}

var fff = closure function( x, y : float ): float
lambda {
    var a, b : int = sumdiff( x, z );
    var s : string = "va va";
}
{ 
  return x*y/(lambda.a + lambda.b)@float
};

x, y, z = 2, 4, 6;

. fff( 2, 3 );
. fff( 5, 3 );
