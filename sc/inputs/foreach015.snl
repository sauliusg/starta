//--*- C -*--

use std;

var a = [ "vienas", "du" ];
var b = [ "eins", "zwei", "drei", "vier", "fünf" ];

for var j = 0 to last(a) do
    var l = a[j];
    . l;
    var count = 0;
    var m: string;
    for var i = 0 to last(b) {
    // for( var i = 0; i < length(b); i++ ) {
        m = b[i];
        . "   ", m;
        count++;
        if( count > 2 ) { break }
    }   
    . "";
enddo
