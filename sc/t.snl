// --*- C -*--

// Test feof() function:

use std;
use Strings;

var count = 0L;

while(<>) {
    var line = fastsplit( $_, ":" );
    for( var word in line ) {
        < "\"" _ word _ "\"" _ " ";
    }
    . "";
    count += llength(line);
}

. count;
