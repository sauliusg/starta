// --*- C -*--

// Test feof() function:

use std;
use Strings;

var count = 0L;

while(<>) {
    var line = split( $_, " ", 0 );
    var separator = "";
    for( var word in line ) {
        < separator _ (word != null ? ("\"" _ word _ "\"") : "(null)");
        separator = ", ";
    }
    . "";
    count += llength(line);
}

. count;
