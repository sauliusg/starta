#
# Regression test:
#

# Check that lists of reference variables are looped correctly:

use std;

type Bag of X = {
     //X value;
     string name;
     int x;
}

. const((Bag of X).name.offset);
//. const((Bag of X).value.offset);
