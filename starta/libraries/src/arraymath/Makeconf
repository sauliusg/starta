COMPILER_SOURCE_DIR ?= ../../..

include ${COMPILER_SOURCE_DIR}/Makeconf

CFLAGS = -I. -I${COMPILER_SOURCE_DIR} -I${COMPILER_SOURCE_DIR}/${INTERPRETER_DIR} -I${COMPILER_SOURCE_DIR}/../cexceptions
LIBFLAGS = -lm
