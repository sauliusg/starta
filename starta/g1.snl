// --*- C -*--

// Test the new generic type implementation (a-la Ada) in interfaces.

use std;

type R = generic ref;

type G = class {
    x : R

    method get() : R
    {
        return self.x;
    }
};

type C = G with (R => string);
