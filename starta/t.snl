#--*- Java -*--
# Starta compiler tests.
#
# Check classes and arrays
#

use std;

class A {
    int i;
    string name;
    long j;

    method set( int x ) { self.i = x }
    method get(): int { return self.i }
}

var a = new A;

. const(A.i.offset);
. const(A.size);
. const(A.nref);
//. const(A.name.offset);

a.i = 10;

//a->set( 20 );

. a->get();
