// --*- C -*--

// Test array comprehension syntax

use std;

var i : int;
// var m = [for i = 1 to 10: i*i];

var m = new string[0];

m = push( m, "vienas" );
m = push( m, "du" );
m = push( m, "trys" );

for var e in m { . e }
