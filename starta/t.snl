#
# Snail compiler tests
#
#
# Test uncheched unsigned int conversions.
#

use std;

function f(int x, y) -> int, int
{
    return x + y, x - y;
}

var p: function (int a; int b) -> (int, int);

p = f;

. f(2,3);
. p(2,3);
