#
# Snail compiler tests
#
#
# Generic lists.
#

use * from std;

use Lists(int) as LI;

var l, p : LI::list of int;

var s : list of int = cons( 1 );

l = s + p;
l = s + cons( 2 );
l = l + cons( 11, cons( 22 )) + cons( 3, cons( 4 ));

< "( ";
foreach var i in l do
    < i, ""
enddo
. ")"
