
use * from std;

var fff = closure function( x, y : float ): float
local {
    var a, b, c = 1, 2, 3;
    var s : string = "va va";
}
{ 
  . local.a; . local.b; . local.c;
  return x - y;
};

. fff( 2, 3 );
. "";

. fff( 5, 3 );
