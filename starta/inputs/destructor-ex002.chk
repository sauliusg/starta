#! /bin/sh

GCC_OPTION=-fsanitize=address

if grep -qE "^OPTFLAGS =.*${GCC_OPTION}" Makeconf
then
    echo -n "Not a genuine leak actually," \
         "this test rather indicates circular calls in desctructors" \
         >&2
    exit 1
else
    exit 0
fi
