#--*- Java -*--
# Starta compiler tests.
#
# Check indexeing of arrays with nominal components.
#

use std;

procedure p( m : array of type T )
{
    var a = m[0:0]; // OK, slice of an array is generated by the runtime for any type.
    var b = m[0]; // ERROR, unknown size and nref of an array component
}
