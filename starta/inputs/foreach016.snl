//--*- C -*--

use std;

var a = [ "vienas", "du" ];
var b = [ "eins", "zwei", "drei", "vier", "fünf" ];

var l: string;
for l in a do
    . l;
    var count = 0;
    var m: string;
    for m in b {
    // for( var i = 0; i < length(b); i++ ) {
        // m = b[i];
        . "   ", m;
        count++;
        if( count > 2 ) { break }
    }   
    . "";
enddo
