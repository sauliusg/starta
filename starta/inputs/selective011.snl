// --*- C -*--
// Check module (package) implementation.
//

// Check whether specific names can be imported from selected
// packages:

import std;
use * from std;

import P1; import P2;

use * from P1;
use * from P2;

. const(C1);
. const(P1::C2);
. const(K1);
. const(P2::K2);

. const(P1::C3);
. const(P2::K3);
. const(P2::C3);
